# 几何数据模块

## 当前进度：V0.1-1 Vec3

`Vec3.hpp` 已实现一个三维向量：保存 `(x, y, z)`，并提供加减、数乘、点积、叉积和长度平方。

本阶段的测试在 `tests/test_vec3.cpp`。它不测试网格，而是确认后续几何计算所依赖的代数规则：

- `a·b` 的结果为标量；
- `a×b` 的结果垂直于 `a` 和 `b`，方向遵从右手定则；
- `(a×b)·c` 是标量三重积，后续 `orient3d` 就会以它为基础。

下一步将创建 `Vertex`，让每一个输入点同时拥有稳定编号和一个 `Vec3` 坐标。
